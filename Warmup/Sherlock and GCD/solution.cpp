#include <iostream>

using namespace std;

int main() {
    int T, N;
    cin >> T;
    while (T--) {
        set <int> A;
        cin >> N;
        for (int i = 0; i < N; i++) {
            cin >> num;
            if (num != 1)
                A.push_bask(num);
        }
        

    }
}